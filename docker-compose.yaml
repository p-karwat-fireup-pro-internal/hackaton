services:
  field-notebook:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: 'field-notebook-backend:latest'
    expose:
      - '3000'
    volumes:
      - 'field-notebook-data:/data'
    environment:
      - 'JWT_SECRET=${SERVICE_BASE64_64_JWT}'
      - 'DATABASE_PATH=/data/app.db'
      - 'PHOTOS_PATH=/data/photos'
      - 'PORT=3000'
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://localhost:3000/health || exit 1']
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    labels:
      - 'traefik.http.services.http-0-eql38a7vbd05at2d0brb7ngz-field-notebook.loadbalancer.server.port=3000'
      - 'traefik.http.routers.http-0-eql38a7vbd05at2d0brb7ngz-field-notebook.service=http-0-eql38a7vbd05at2d0brb7ngz-field-notebook'

volumes:
  field-notebook-data:
